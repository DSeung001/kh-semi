<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zzanmat.tour.post.mapper.PostLikeMapper">

        <select id="countByPostId" resultType="int">
            SELECT COUNT(*)
            FROM POST_LIKE
            WHERE POST_ID = #{postId}
        </select>

    <select id="existsByPostIdAndUserId"
            resultType="boolean">
        SELECT COUNT(*) > 0
        FROM POST_LIKE
        WHERE POST_ID = #{postId}
            AND USER_ID = #{userId}
    </select>

    <insert id="save">
        INSERT INTO POST_LIKE (
            POST_ID,
            USER_ID,
            CREATE_AT
        )
        VALUES (
            #{postId},
            #{userId},
            NOW()
        )
    </insert>

    <delete id="deleteByPostIdAndUserId">
        DELETE FROM POST_LIKE
        WHERE POST_ID = #{postId}
            AND USER_ID = #{userId}
    </delete>

</mapper>